1) Creazione DATABASE e load dati

use testJdom;
create table testuser (
       userId           char(20) not null,
       name             varchar(20) null,
       age              int(5) default NULL,
       PRIMARY KEY  (userId)
);
INSERT INTO testuser VALUES ('1', 'tony',20);
INSERT INTO testuser VALUES ('2', 'sunny',25);
INSERT INTO testuser VALUES ('3', 'kevin',35);

CREATE TABLE upload (
  objectId            char(50) NOT NULL default '',
  name                varchar(50) default '',
  description         varchar(200) default '',
  datas               LONGBLOB,
  size                int(20) NOT NULL default '0',  
  messageId           varchar(20) NOT NULL default '0',
  creationDate        VARCHAR(15) NOT NULL,
  contentType         varchar(50) default '',
  PRIMARY KEY  (objectId),
  KEY messageId (messageId)
)

2) DEFINIZIONE DATASOURCE
Sul file: C:\runtime\apache-tomcat-8.0.36\conf\context.xml

inserire il fragment che definisce del datasource che viene risolto da web.xml
<Context>
.....
.....
<Resource name="jdbc/TestDS" auth="Container" type="javax.sql.DataSource"
               maxTotal="100" maxIdle="30" maxWaitMillis="10000"
               username="root" password="root" driverClassName="com.mysql.jdbc.Driver"
               url="jdbc:mysql://localhost:3306/testjdom"/>
			   
</Context>

3) DEFINIZIONE DATASOURCE

copiare al percorso C:\runtime\apache-tomcat-8.0.36\lib la libreria driver JDBC 
per MYsql: mysql-connector-java-5.1.38-bin.jar

